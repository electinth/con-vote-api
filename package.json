{
  "name": "con-vote-api",
  "description": "API for con-vote project to fetch and cache data from Airtable",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"tsc -w\" \"nodemon src/index.js\"",
    "build": "tsc",
    "serve": "node dist/index.js"
  },
  "dependencies": {
    "airtable": "^0.10.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "polka": "^0.5.2",
    "typescript": "^4.3.4"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7"
  }
}
